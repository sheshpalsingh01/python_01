first of all 
Create a Repo don't marked Readme file leave it only click on Create

Command
 git init 
 dir
Cheack globle config
 git config --global user.name
 git config --global user.email
  
  git status { Check for git status}
  git add {file name eg.= xyz }
  git status

Stagging Enverment
 git commit -m "this is message like first commit "
 git status

for the branch 
 git branch
 git branch -M main
 git branch {branch name} [ this is for Creating a New branch]
 git checkout { beanch name} [ Use for switch branch like main branch to { beanch name} ]
 git status
 git add "Developer_branch.txt"
 git commit -m "message"
 git push origin Developer 
 git merge {branch name}
 

local file connect to the git 
 git remote add origin https://github.com/sheshpalsingh01/python_01.git
 git status
 git remote -v 
 git push origin main

For Restore the file 
 git restore {file name you want to restore}

Both file Goes on git
 git add .


Git Clone 
 Create a new folder or file
 git clone https://github.com/sheshpalsingh01/python_01.git ( this link from git repo code section)